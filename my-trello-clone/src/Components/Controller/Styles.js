// Módulos externos
import { makeStyles } from "@material-ui/core/styles";

// Classe de estilização => ControllerStyle
export const controllerStyle = makeStyles((theme) => ({
    root:{    
      display: "flex",
      minHeight: "100vh",
      width: "100%",
      overflowY: "auto"
    }
}));
  